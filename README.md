# QR Code Generator
This library is a simple generator of QR codes. It supports all QR code versions (1 - 40), 
all error correction levels and all mask types.
It also supports the following data encoding types:
* Numeric
* Alphanumeric
* Bytes
* Kanji